{% extends 'base.html.twig' %}

{% block title %}TUserUsr{% endblock %}

{% block body %}
    <h1>TUserUsr</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_user_usr.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ t_user_usr.type }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ t_user_usr.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ t_user_usr.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_t_user_usr_index') }}">back to list</a>

    <a href="{{ path('app_t_user_usr_edit', {'id': t_user_usr.id}) }}">edit</a>

    {{ include('t_user_usr/_delete_form.html.twig') }}
{% endblock %}
