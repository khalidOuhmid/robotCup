<form method="post" action="{{ path('app_t_user_usr_delete', {'id': t_user_usr.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_user_usr.id) }}">
    <button class="btn">Delete</button>
</form>
