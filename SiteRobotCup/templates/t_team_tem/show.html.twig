{% extends 'base.html.twig' %}

{% block title %}TTeamTem{% endblock %}

{% block body %}
    <h1>TTeamTem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_team_tem.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ t_team_tem.name }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ t_team_tem.score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_t_team_tem_index') }}">back to list</a>

    <a href="{{ path('app_t_team_tem_edit', {'id': t_team_tem.id}) }}">edit</a>

    {{ include('t_team_tem/_delete_form.html.twig') }}
{% endblock %}
