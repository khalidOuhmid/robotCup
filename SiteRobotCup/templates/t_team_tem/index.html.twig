{% extends 'base.html.twig' %}

{% block title %}TTeamTem index{% endblock %}

{% block body %}
    <h1>TTeamTem index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Score</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for t_team_tem in t_team_tems %}
            <tr>
                <td>{{ t_team_tem.id }}</td>
                <td>{{ t_team_tem.name }}</td>
                <td>{{ t_team_tem.score }}</td>
                <td>
                    <a href="{{ path('app_t_team_tem_show', {'id': t_team_tem.id}) }}">show</a>
                    <a href="{{ path('app_t_team_tem_edit', {'id': t_team_tem.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_t_team_tem_new') }}">Create new</a>
{% endblock %}
