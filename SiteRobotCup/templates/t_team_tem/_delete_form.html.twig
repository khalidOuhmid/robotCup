<form method="post" action="{{ path('app_t_team_tem_delete', {'id': t_team_tem.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_team_tem.id) }}">
    <button class="btn">Delete</button>
</form>
