<header class="bg-gray-800 text-white">
  <nav class="container mx-auto flex items-center justify-between p-4">
    <!-- Logo -->
    <a href="{{ path('app_default') }}" class="text-3xl font-bold hover:text-gray-400">
      RoboCup
    </a>

    <!-- Desktop Navigation -->
    <div class=" md:flex space-x-4">
      <a href="{{ path('app_default') }}" class="text-2xl hover:text-gray-400">Accueil</a>
      <a href="{{ path('app_default') }}" class="text-2xl hover:text-gray-400">Score</a>
    </div>
  </nav>


</header>
