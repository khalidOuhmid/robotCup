<form method="post" action="{{ path('app_t_member_mbr_delete', {'id': t_member_mbr.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_member_mbr.id) }}">
    <button class="btn">Delete</button>
</form>
