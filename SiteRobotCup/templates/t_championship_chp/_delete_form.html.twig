<form method="post" action="{{ path('app_t_championship_chp_delete', {'id': t_championship_chp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_championship_chp.id) }}">
    <button class="btn">Delete</button>
</form>
