{% extends 'base.html.twig' %}

{% block title %}Tableau des scores{% endblock %}

{% block body %}
<div class="container">
    <h1>Tableau des scores</h1>
    <table class="table">
        <thead>
            <tr>
                <th>Position</th>
                <th>Équipe</th>
                <th>Score Total</th>
                <th>Victoires</th>
            </tr>
        </thead>
        <tbody>
        {% for team in teams %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ team.name }}</td>
                <td>{{ team.calculateScore }}</td>
                <td>{{ team.victoryCount }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
